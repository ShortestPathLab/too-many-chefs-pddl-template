"""Your PDDL controller: the domain, the two classes, and your helpers.

``overcooked.pddl`` is the domain, ``problem_generator.py`` builds problems
for it, and ``from_pddl.py`` turns the solver's plans back into mutations.
``demo.py`` is the example the starter code runs; it can go once nothing
imports it.

Add modules of your own beside these and import them, for example
``from controllers.pddl.submission.grid import neighbours``. The contest
server grades this folder and nothing else. See "The Files You Will Edit" in
getting-started.md.
"""
